---
# Full fleet convergence over every Ansible-managed host class.
#
#   fleet ansible run site
#
# Each per-group playbook is self-contained — running site.yml is equivalent
# to running them in this order.

- import_playbook: pve.yml
- import_playbook: pbs.yml
- import_playbook: developer.yml
