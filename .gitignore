# Nix
result
result-*
.direnv/

# Python / launcher venv
.venv/
__pycache__/
*.pyc
*.egg-info/

# fleet CLI runtime artifacts (generated; never commit)
.cache/
.logs/
.env

# Decrypted secrets must never land in git
*.plaintext.yaml

# Local mdBook preview output (CI builds via nix)
docs/book/
