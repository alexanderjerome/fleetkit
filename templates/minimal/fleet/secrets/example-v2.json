{
  "default": {
    "api_token": "ENC[AES256_GCM,data:placeholder,iv:placeholder,tag:placeholder,type:str]"
  },
  "sops": {
    "note": "placeholder shape only — run `fleet secrets init` and re-encrypt with your own age key before first deploy"
  }
}
